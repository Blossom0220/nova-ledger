server:
  port: 8081

spring:
  application:
    name: nova-ledger

  datasource:
    url: jdbc:mysql://localhost:3306/nova_ledger?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&createDatabaseIfNotExist=true
    username: root
    password: lyz200607
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.MySQLDialect
        show_sql: true

  data:
    redis:
      host: localhost
      port: 6379
      timeout: 5000ms

  cache:
    type: redis

# JWT Configuration
jwt:
  secret: nova-ledger-secret-key-2026-change-me-in-production
  expiration: 86400000

logging:
  level:
    com.nova: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: WARN
    org.springframework.web.servlet: DEBUG
